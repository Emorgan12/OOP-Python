# 1. If - Assign 5 to x. If x is bigger than 0, print "x is a positive number".

# 2. If-else - Assign -5 to y. If x is bigger than 0, print "y is a positive number". Else, print "y is a negative number".

# 3. If-elif-else - Assign 0 to z. If z is bigger than 0, print "z is a positive number". Else if z is zero, print "z is 0". Else, print "z is a negative number".


# pseudocode flowchart and python programming
# Assign 99 to a. If a is bigger than 10, 20, 30, 40, 50, 60, 70, 80, or 90 prints which group "a" fall into?



